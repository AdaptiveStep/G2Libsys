<UserControl x:Class="G2Libsys.SearchBarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:G2Libsys.Controls"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="700">
    
    <Grid Background="{StaticResource primary_dark}">
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="4*"/>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <TextBox
                   Grid.Column="1"
                   BorderThickness="2"
                   Margin="0 50 0 0"
                   Background="{StaticResource light_background}"
                   materialDesign:HintAssist.Hint="Ange ett eller flera sökord"
                   Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                   VerticalAlignment="Center"
                   Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
            >
            <TextBox.InputBindings>
            <!--Clear vid esc-->
                <KeyBinding Command="{Binding ClearSearchCommand}" Key="Esc" />
            <!-- Sök vid enter -->
                <KeyBinding Command="{Binding SearchCommand}" Key="Return" />
                </TextBox.InputBindings>
            </TextBox>
        
        <ComboBox
                   Grid.Column="2"
                   Margin="0 50 0 0"
                   Foreground="{StaticResource light_background}"
                   FontWeight="Bold"
                   Background="{StaticResource accent_light}"
                   materialDesign:HintAssist.Hint="Böcker"
                   
                   Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                   materialDesign:HintAssist.HelperText="Vad letar du efter?" >
            <ComboBoxItem>
                Seminarium
            </ComboBoxItem>
            <ComboBoxItem>
                Anställda
            </ComboBoxItem>
            <ComboBoxItem>
                Mina Lån
            </ComboBoxItem>
            
        </ComboBox>

        <Button 
                   x:Name="SearchButton"
                   Grid.Column="3"
                   Height="40"
                   Width="55"
                   HorizontalAlignment="Left"
                   Margin="0 50 0 0"
                   Content="Sök"
                   Command="{Binding SearchCommand}"
        >
        </Button>
        
    </Grid>
</UserControl>
